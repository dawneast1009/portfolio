#!/usr/bin/env ruby
# frozen_string_literal: true
# Every test creates its own temporary storage; real content is not modified.
Dir[File.expand_path('../test/*_test.rb', __dir__)].sort.each { |path| require path }
