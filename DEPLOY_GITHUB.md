# 무료 공유 및 기존 배포 업데이트

현재 버전의 전체 설명은 README.md의 4~5절을 사용하세요.

**추천: 컴퓨터에서 Ruby로 작성 → 공유 및 제출 → 제출용 ZIP 다운로드 → ZIP 내부 파일만 별도 공개 저장소에 업로드 → Settings / Pages / Deploy from a branch / main / root → 실제 주소 제출**

작성용 Ruby 소스는 GitHub Pages에서 실행되지 않습니다. Pages에 올리는 것은 내보낸 읽기 전용 HTML 제출본입니다. Pages에 올린 파일은 공개되므로 민감한 개인정보를 넣지 마세요.

이미 Render에 올린 소스를 바꾸는 경우 새 ZIP 내부 파일을 기존 저장소의 동일한 위치에 반영하세요. 저장소 첫 화면에 Gemfile, config.ru, lib, views, public이 보여야 합니다. .env와 실제 storage 폴더는 올리지 않습니다.

이번 버전은 render.yaml과 render-free.yaml 모두 Free 설정입니다. 이전 버전의 유료 render.yaml 설명은 더 이상 사용하지 않습니다. 무료 서버에서 입력한 내용과 파일은 재시작·재배포·유휴 종료에 초기화되므로 최종 결과물과 편집 원본은 별도로 보관하세요.

실제 사용자 저장소 업로드나 외부 서비스 배포는 이번 작업에서 수행하지 않았습니다.
